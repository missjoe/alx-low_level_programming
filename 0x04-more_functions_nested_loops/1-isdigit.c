#include "main.h"
/**
 * _isdigit - This is the function name
 * @c: the int to be checked
 * Description: function that checks for a digit (0 through 9).
 * Return: 1 if c is uppercase or 0 if otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
