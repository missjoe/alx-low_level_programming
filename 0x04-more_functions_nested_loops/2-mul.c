#include "main.h"
/**
 * mul - This is the function name
 * @a: the int to be multiplied to b
 * @b: the int to be multiplied to a
 * Return: 0
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
